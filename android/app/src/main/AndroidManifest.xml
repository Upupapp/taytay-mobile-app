<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The app talks to the Taytay LGU IDS API over HTTPS. Flutter injects this
         into debug and profile builds automatically but not into release, so a
         release APK without it cannot reach the backend at all. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Local app unlock only: re-showing an already signed-in app after it has
         been backgrounded. No biometric data reaches this app or the network —
         the platform returns a boolean. Never used as identity proof and never
         sent to the server. See `lib/core/session/app_lock_controller.dart`. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>

    <!-- Deliberately NO `android.permission.CAMERA`, and no media permission.
         Both omissions are load-bearing rather than oversights:

         * Camera — `image_picker` uses `ACTION_IMAGE_CAPTURE`, which hands the
           work to the system camera app. Declaring `CAMERA` would *add* a
           runtime grant this app does not otherwise need: once the permission is
           in the manifest, Android requires it to be granted before that intent
           will return a photo. Leaving it out means one fewer permission prompt
           for the resident and one fewer capability in the APK.
         * Photos — gallery selection goes through the Android photo picker,
           which returns the single item the resident chose. `READ_MEDIA_IMAGES`
           would grant this app the entire photo library to read one document,
           which is the opposite of the minimisation CLAUDE.md Article 5 asks for.
    -->

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
